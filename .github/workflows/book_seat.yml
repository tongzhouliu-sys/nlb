name: NLB Seat Auto-Booking

on:
  # 每天 10:00 SGT (= 02:00 UTC) 自动触发
  schedule:
    - cron: "0 2 * * *"

  # 也可在 Actions 页面手动触发（方便测试）
  workflow_dispatch:
    inputs:
      date_offset:
        description: "预约哪天（0=今天, 1=明天, 默认1）"
        required: false
        default: "1"

jobs:
  book-seat:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      # ── 1. 拉取代码 ───────────────────────────────────────────────
      - name: 📥 Checkout repository
        uses: actions/checkout@v4

      # ── 2. 配置 Python ────────────────────────────────────────────
      - name: 🐍 Set up Python 3.12
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip

      # ── 3. 安装依赖 ───────────────────────────────────────────────
      - name: 📦 Install dependencies
        run: |
          pip install -r requirements.txt
          playwright install chromium
          playwright install-deps chromium

      # ── 3.5 安装完环境后随机等待（30–240 秒） ────────────────────
      - name: ⏳ Random delay after setup (30–240s)
        run: |
          DELAY=$(( RANDOM % 211 + 30 ))
          echo "Sleeping for ${DELAY} seconds before booking..."
          sleep "$DELAY"

      # ── 4. 执行预约脚本（第一个座位） ────────────────────────────
      - name: 🪑 Run booking script
        env:
          NLB_USERNAME: ${{ secrets.NLB_USERNAME }}
          NLB_PASSWORD: ${{ secrets.NLB_PASSWORD }}
          BOOKING_DATE_OFFSET: ${{ github.event.inputs.date_offset || '1' }}
        run: python book_seat.py

      # ── 4.5 预约完第一个后随机等待（5–120 秒） ───────────────────
      - name: ⏳ Random delay after first booking (5–120s)
        run: |
          DELAY=$(( RANDOM % 116 + 5 ))
          echo "Sleeping for ${DELAY} seconds after first booking..."
          sleep "$DELAY"

      # ── 5. 上传截图（失败时也上传，便于排查） ─────────────────────
      - name: 📸 Upload screenshots (debug)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: booking-screenshots-${{ github.run_id }}
          path: screenshots/
          retention-days: 7
          if-no-files-found: ignore
